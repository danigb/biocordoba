- title "Nueva configuración"

- form_for :preference, :url => preferences_path do |f|
  #preferences
    = render :partial => 'form', :locals => {:f => f}
  %p= f.submit "Guardar"

%p= link_to "Volver al listado", preferences_path
